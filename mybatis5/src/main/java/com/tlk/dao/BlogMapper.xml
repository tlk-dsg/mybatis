<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tlk.dao.BlogMapper">

    <insert id="addBlog" parameterType="Blog">
        insert into mybatis.blog
        value(#{id},#{title},#{author},#{createTime},#{views})
    </insert>

    <select id="queryBlogIf" parameterType="Map" resultType="Blog">
        select * from mybatis.blog where views > 0
        <if test="title != null">
            and title = #{title}
        </if>
        <if test="author != null">
            and author = #{author}
        </if>
    </select>

    <select id="findActiveBlogLike" resultType="Blog">
        SELECT * FROM mybatis.blog WHERE 1=1
        <choose>
            <when test="title != null">
                AND title like #{title}
            </when>
            <when test="author != null">
                AND author like #{author}
            </when>
            <otherwise>
                AND views > 0
            </otherwise>
        </choose>
    </select>

    <select id="selectPostIn" parameterType="Map" resultType="Blog">
        select * from mybatis.blog
        <where>
            <foreach collection="views" item="view" open="and (" close=")" separator="or">
                views = #{view}
            </foreach>

        </where>
    </select>
</mapper>